# 智一盈小账 · WiseLedger — 使用说明

## 安装依赖

```bash
pip install PySide6 openpyxl cryptography keyring
```

## 启动程序

```bash
python main.py
```

## 登录

程序启动后会显示登录框。默认超级管理员账号：

| 用户名 | 密码 |
|--------|------|
| admin | admin123 |

首次登录后建议立即在【系统管理】中修改密码。

## 用户角色与权限

| 角色 | 说明 |
|------|------|
| 超级管理员（superadmin） | 全部权限，包含用户管理 |
| 管理员（admin） | 除用户管理外的全部功能 |
| 会计（accountant） | 查看客户、科目、凭证录入、报表查看/导出 |
| 只读（readonly） | 仅可查看客户、科目、凭证、报表 |

会计和只读角色只能访问被授权的客户账套，由超级管理员在【系统管理】中配置。

## 功能模块

### 👥 客户管理
- 新建/编辑/删除客户账套
- 存储客户名称、助记码、税号、客户类型（一般纳税人/小规模纳税人）、会计准则（企业会计准则/小企业会计制度）、联系人信息
- 支持按名称/助记码搜索
- 点击客户行的"打开账套"进入该客户的记账流程
- 支持导入已有账套（.zyac 加密备份文件）

### 📋 科目管理
- 查看当前账套的会计科目树（支持多级展开）
- 管理员及以上可新增/编辑/停用科目
- 支持为末级科目绑定辅助核算维度（如客户、供应商等）

### 📂 科目期初
- 在建账期或每年 1 月录入末级科目的期初借方/贷方余额
- 非建账期和非 1 月禁止录入，防止误操作

### 📝 记账（凭证）
凭证页包含 5 个标签：

- **新增凭证**：填写凭证日期、摘要、科目、借贷金额，支持辅助核算填写。凭证模板可快速套用常用分录（工资、采购、销售、税费等共 25 个预置模板）。
- **查凭证**：按期间筛选，查看凭证列表；可编辑/删除待审核凭证；管理员可审核或撤销审核。已封账期间的凭证不可修改。
- **科目余额表**：汇总当前期间各科目借贷发生额和期末余额（仅已审核凭证）。
- **明细账**：按科目查看指定期间的逐笔明细。
- **辅助核算**：按辅助核算维度（如往来单位、员工等）查看发生额明细。

凭证状态流转：**待审核 → 已审核 / 已拒绝**  
只有"已审核"凭证才计入报表和科目余额表。

### 🔚 期末结账
1. 选择结账期间
2. 点击"生成结转凭证"：系统自动生成收入/费用结转到本年利润的凭证（状态直接为已审核）
3. 点击"结账封账"：封账后该期间凭证全部锁定，不可增删改
4. 若需修改：点击"反结账"解除封账，修改完毕后重新结账

封账前检测：若存在未审核凭证，系统会阻止封账。

### 📈 财务报表
报表页包含 5 个标签：

- **资产负债表**：截止选定期末的资产、负债、所有者权益汇总
- **利润表**：选定期间的收入、成本、费用、利润明细
- **所有者权益变动表**
- **现金流量表**
- **收支统计表**：多期间收支环比分析

所有报表均支持导出 Excel（.xlsx）。报表数据仅汇总"已审核"凭证。

### 🔍 审计日志
- 按客户、日期范围、操作类型查询所有关键操作记录
- 记录内容包括：登录/退出、新增/编辑/删除凭证、审核操作、结账、反结账、备份恢复等
- 支持导出 Excel

### ⚙️ 系统管理（仅超级管理员）
- **用户管理**：新增/编辑/停用用户，设置角色
- **客户授权**：为会计/只读用户分配可访问的客户账套
- **手动备份**：将数据库加密导出为 `.zyac` 文件（AES-256-GCM 加密）
- **恢复备份**：从 `.zyac` 文件恢复数据库
- **自动备份**：设置月末自动备份开关及备份目录；备份密码存储于系统凭据管理器（Windows Credential Manager / macOS Keychain）

## 自动备份说明

自动备份在每次登录时触发"补跑"检测：若有已过期月份尚未备份，会静默执行并写审计日志。用户无需在月末最后一天登录，最多回溯 6 个月。

## 数据文件

所有数据保存在程序同目录下的 `accounting.db`（SQLite 数据库）。

**建议定期使用【系统管理 → 手动备份】创建加密备份！**

## 打包成可执行文件

```bash
pip install pyinstaller
pyinstaller 智一会计.spec
```

或手动指定：

```bash
pyinstaller --onefile --windowed --name 智一盈小账 \
    --add-data "arrow_dn.svg:." --add-data "WiseLedger.ico:." main.py
```
